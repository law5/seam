# seam 仕様書

## 目的

2話者がマイク別々に収録したPodcast素材（音声ファイル×2）を、Premiere Proでの本編集の**前処理**として整える。
最終的な編集判断（間の作り方・被りの直し方）は人間が行う。**ツールは検出と可視化と機械的な調整までを担い、勝手に会話の間を作り変えない。** 自動調整（F7）も、ユーザーが明示的に選んで実行する opt-in 機能であり、dry-run プレビューと Undo で編集判断はユーザーに残る。

## 入出力

### 入力
- 音声ファイル×2（話者A/話者B、マイク別録音）
  - 受け付ける形式: MP3 / WAV / M4A / AAC / FLAC / OGG / Opus / AIFF（ffmpeg が扱える一般的な音声形式の許可リスト。正は `server.ALLOWED_AUDIO_EXTS`）
  - 内部処理は取込時に 48kHz/16bit/mono の PCM(WAV) へ揃える
- 2ファイルは同時録音前提だが、開始時刻のズレがありうる（頭合わせはUI上で手動オフセット調整）

### 出力（エクスポート一式・出力先ディレクトリ指定可）
- `speakerA.wav` / `speakerB.wav` — 全処理＋タイムライン編集を反映した 48kHz/16bit モノラル WAV×2（デフォルト）
  - エクスポート時にフォーマット選択可: WAV（Premiere本編集用・無劣化）/ MP3 320kbps（容量優先・仮編集用）
  - 内部処理は常にPCM(WAV)で行い、MP3は最終書き出し時のみエンコード（劣化の重ね掛けを避ける）
- `transcript.srt` — 文字起こし（話者ラベル付き、編集後タイムラインに追従）。**文字起こし済みのときのみ生成**
- `overlaps.csv` — 被り区間リスト（`start, end, duration` の3列）
- 編集状態は作業フォルダの `project.json` に保存される（エクスポート一式には**既定で含まれない**。API の `bundle="reeditable"` 指定時のみ、素材とともに同梱される）

## 機能要件

### F1. 音圧調整（2話者バランス）
- 取込時の正規化は **opt-in（既定OFF）**。有効にすると各トラックへ EBU R128 ラウドネス解析を実施し、目標 **-16 LUFS**（変更可能な設定値）へ正規化する（ffmpeg `loudnorm` 3パス: 計測 → 適用 → 検証）。取込後も「このトラックを正規化」で個別に実行できる
- UIにトラック別ゲインスライダー（-12〜+12 dB）。スライダー変更はプレビュー再生に即時反映
- 受け入れ条件: 正規化後の2トラックの integrated loudness 差が ±1 LU 以内

### F2. 歯擦音の調整（ディエッサー）
- ffmpeg `deesser` フィルタをトラック別に適用。強度（i）をUIスライダーで調整
- before/after を同一区間でA/B試聴できること
- 受け入れ条件: 強度0で原音と一致（バイパス）、強度変更が試聴に反映される

### F3. 被り検出
- 各トラックにVAD（発話区間検出）をかけ、**両トラックの発話が時間軸で重なる区間**を被りとして検出
- 被り区間は波形上に色付きハイライト＋サイドパネルに一覧（クリックでジャンプ）
- 最小被り長（デフォルト300ms、設定可）未満は無視（息・相槌のノイズ除け）
- 受け入れ条件: 一覧とハイライトが一致し、ジャンプで該当区間が再生ヘッド位置になる
- 解消手段は、ユーザーの手による F4 のブロック操作 / トラックゲイン、または **F7 の opt-in 自動調整**

### F4. 発話ブロックのタイムライン編集（本ツールの中核）
- VADで検出した発話区間を「ブロック」として、2トラックのタイムライン上に**クリップとして表示**する（各クリップは自分の波形を持ち、移動・分割・削除が波形表示へそのまま反映される = 見えている編集状態がエクスポート結果と一致するWYSIWYG。ブロックはソース座標 `source_start`/`source_end` と出力タイムライン座標 `start` を持つ）
- ブロック操作:
  - **ドラッグ移動**: 時間軸方向に前後へ動かす（被り解消・間の調整）
  - **間を開ける**: ①ブロックを掴んで後ろへズラす ②タイムライン上の任意点を右クリック→「ギャップ挿入（n秒）」で以降のブロックを一括後退（トラック単独/両トラック選択可）
  - **間を詰める**: 隣接ブロック間の無音を右クリック→「ギャップ削除」
  - **削除**: ブロック選択→Deleteで除去（除去区間は無音化）
  - **分割**: 再生ヘッド位置でブロックを2分割
- 各ブロックに文字起こしテキストの先頭を表示（F5連携）。テキスト検索でブロックへジャンプ
- Undo/Redo（最低20段）
- 受け入れ条件: 全操作がエクスポートWAVに正確に反映される。移動・削除でクロスフェード（10ms、設定可）を適用しクリックノイズを出さない

### F5. 文字起こし
- Whisper（ローカル実行、モデルはmedium以上をデフォルト・設定で変更可）でトラック別に文字起こし
- モデルは `scripts/fetch_whisper_model.sh` でセットアップ時に一度ダウンロードして既定パスへ配置し、運用時はオフラインで実行する（音声が外部送信されることはなく、ダウンロードされるのはモデル重みのみ）
- 結果は発話ブロックに紐付け、SRT書き出し（話者ラベル `A:` / `B:` 付き）
- ブロック移動後のSRTは**編集後のタイムスタンプ**で出力
- 受け入れ条件: SRTがPremiere Proのキャプションインポートで読めること

### F6. プロジェクト保存/復元
- 編集状態（オフセット・ゲイン・ディエッサー設定・ブロック配置・文字起こし）を `project.json` に保存
- 起動時に既存プロジェクトを開ける

### F7. opt-in 自動調整

「無音の自動詰め・被りの自動解消」は、**編集判断はユーザーに残す原則との折衷**として次の形で提供する: **チェックボックスによる opt-in + dry-run プレビュー + Undo 1段**。勝手には実行されず、何が変わるかを見てから適用し、1操作で丸ごと戻せる。

- 対象はチェックボックスで個別に選択:
  - **無音を詰める**: 両話者とも無音の `max_gap_s` 超（既定1.5秒）のギャップを `keep_gap_s`（既定0.5秒）まで左詰め。片方の話者が発話中の区間はギャップ扱いしない
  - **被りを解消**: 後発ブロックが先行ブロックの終端を越える tail 型の被り（検出済み一覧と同じ `min_overlap_s` 以上）について、カット点以降の**両話者の全ブロック**を等量後退させ、後発を先行の終端に接続する（会話の相対タイミングを保存）
- 自動対象外（スキップ理由つきで報告）: 相槌などの内包被り／`max_overlap_s`（既定3.0秒）超の長い同時発話／同時開始の被り
- **dry-run プレビュー必須動線**: 適用前に対象件数・短縮秒数と、対象区間のタイムライン上ハッチ表示で「何が消えるか」を確認できる
- 適用は Undo 1段（既存の Undo/Redo 履歴に1操作として積む）
- ブロックが動くのは出力タイムライン座標 `start` のみ。ソース座標は不変で、文字起こし・SRT はブロックと一緒に平行移動する
- 受け入れ条件: 適用後、対象ギャップは `keep_gap_s` に収まり、tail 型かつ `max_overlap_s` 以下の被りが 0 件になる。同じ設定での2回目実行は無変化（冪等）

## 非機能要件

- ローカル完結。外部API送信なし（Whisperもローカル）
- 90分×2トラックの素材で、取込解析（VAD+ラウドネス+文字起こし）が実用時間内（目安: M系Mac実機で15分以内。文字起こしは進捗表示＋バックグラウンド実行で、完了前でも波形編集を開始できること）
- 波形表示は90分素材でスクロール・ズームが滑らかであること（ピークデータの事前計算）

## 技術スタック（推奨・合理的な代替は提案可）

- バックエンド: Python 3.12+ / FastAPI / uv管理
- 音声処理: ffmpeg（loudnorm, deesser, 切り貼りはfilter_complexまたはPCM直接操作）
- VAD: silero-vad（torch）または webrtcvad。精度と導入の軽さで選定理由を添えること
- 文字起こし: faster-whisper または whisper.cpp バインディング
- フロント: 自前canvas描画（クリップ式波形・仮想スクロール）+ Web Audio 再生エンジン + 素のJS（ES modules・ビルドなし・外部CDN依存なし）。外部の波形ライブラリには依存しない
- ポート: 4520（環境変数で変更可）

## UIワイヤー（概略）

```
┌──────────────────────────────────────────────┐
│ [プロジェクト名]  [取込] [保存] [エクスポート]      │
├──────────────────────────────────────────────┤
│ ▶ ⏸ ⏪ ⏩   00:12:34 / 01:30:00   zoom [-][+]  │
├──────────────────────────────────────────────┤
│ A ────▆▆▆▆──────▆▆▆▆▆▆──▆▆──────▆▆▆▆──    │ ← ブロック=ドラッグ可
│ B ──────────▆▆▆▆──────▆▆▆▆▆▆▆──────▆▆▆──  │    被り=赤ハイライト
├──────────────┬───────────────────────────────┤
│ 被り一覧      │ トラック設定                     │
│ 12:34 (0.8s) │ A: gain[──●──] deess[─●───]    │
│ 23:45 (1.2s) │ B: gain[───●─] deess[──●──]    │
│ ...          │ offset: A +0ms / B +120ms       │
└──────────────┴───────────────────────────────┘
```

## スコープ外（やらないこと）

- ノイズリダクション・BGM/ジングル合成（Premiere側の仕事）
- 3話者以上・クラウド連携・認証

※ 「被りの自動解消・無音の自動詰め」は F7 の opt-in 自動調整として提供する（チェックボックス + dry-run プレビュー + Undo 1段により「編集判断はユーザーに残す」原則は維持）。

## 完成の定義（Definition of Done）

1. 実機の音声ファイル×2（MP3 / WAV）で 取込→F1〜F7の操作→エクスポート が一連で動作する
2. エクスポートWAVをPremiere Proに読み込み、SRTキャプションと位置が一致する
3. 受け入れ条件（各機能に記載）を満たす
4. README に起動・操作手順が記載されている
5. 主要処理（VAD区間→ブロック編集→出力タイムスタンプ変換）に単体テストがある
