# セキュリティについて

## このアプリの前提

**seam はローカル専用ツールです。認証機構を持ちません。** 「そのマシンを使っている人 = 正当な利用者」という前提で動きます。

安全性は、次の1点に依存しています:

> **サーバが `127.0.0.1` にしかバインドされていない = そのマシンからしかリクエストが届かない**

この前提が崩れると、アクセス制御上の境界は残りません（後述の Host / Origin 検証は残りますが、迂回できます）。

- 音声ファイルを外部へ送信することはありません。文字起こしは faster-whisper のローカル実行です
- UI は外部 CDN を読み込みません

## Host / Origin 検証について（重要）

**Host はすべてのリクエストで**、**Origin は状態を変更するリクエストで**（ヘッダが付いている場合に）検証しています。ただし、**これはブラウザ経由の攻撃に対する緩和策であって、アクセス制御ではありません。**

何を防ぐか:

- **DNS rebinding** — 攻撃者のドメインが `127.0.0.1` に解決されるよう仕込まれても、Host ヘッダが許可リストに無ければ 403。これは応答を読み取る攻撃なので、GET を含む全メソッドで検証しています
- **CSRF** — 他サイトが開かせたリクエストは、Origin が一致しなければ 403

何を防がないか:

- **Host ヘッダの詐称** — ブラウザは Host を詐称できませんが、`curl` やスクリプトは自由に設定できます。ループバック以外にバインドしている場合、ネットワーク上の誰かが `Host: 127.0.0.1:4520` を載せてリクエストすれば検証は通過します
- **Origin ヘッダを送らない経路** — Origin が無いリクエストは意図的に通しています（CLI・スクリプトからの API 利用を壊さないため）。つまり非ブラウザ経路は Origin 検証を素通りします

**結論として、Host / Origin 検証はブラウザを踏み台にした攻撃を止めるためのものです。ネットワーク到達性そのものを制限する仕組みではありません。**

## 気をつけてほしいこと

**`SEAM_HOST`（旧 `PODCAST_PREP_HOST`）をループバック以外に設定しないでください。**

`0.0.0.0` でも、具体的なIPアドレス（例 `192.168.1.10`）でも、**結果は同じです** — 認証のない全 API が、そのネットワークから到達可能になります。上記のとおり Host 検証は詐称で迂回できるため、「`0.0.0.0` なら Host 検証で守られる」ということはありません。

別のマシンから使いたい場合は、バインド先を変えるのではなく、SSH ポートフォワードなど**認証のある経路**を挟んでください。

```bash
# 別マシンから使う例（リモート側のポートを手元へ転送する）
ssh -L 4520:127.0.0.1:4520 user@the-machine-running-seam
```

## 脆弱性を見つけたら

**GitHub の [Security Advisories](https://github.com/law5/seam/security/advisories/new) から非公開で報告してください。** Issue は公開されるため、脆弱性の報告には向きません。

個人が趣味の範囲で運用しているツールのため、対応や返信をお約束することはできませんが、内容は必ず確認します。

脆弱性ではない不具合・要望は、通常どおり [Issue](https://github.com/law5/seam/issues) へどうぞ。

## 既知の設計上の制約

- **認証がない** — 上記のとおり、ローカル単一ユーザーを前提とした設計です
- **書き込み先はユーザーが選ぶ** — 取込時に指定した作業フォルダに、音源・中間ファイル・書き出しが保存されます。アプリが生成したファイル以外には触れません
- **クラウド同期フォルダは作業フォルダに選べません** — 同期クライアントが書き込み途中の中間ファイルを掴むとデータが壊れるためです
