# セキュリティについて

## このアプリの前提

**seam はローカル専用ツールです。** 認証機構は持たず、「そのマシンを使っている人＝正当な利用者」という前提で動きます。

- サーバは既定で `127.0.0.1`（localhost）にのみバインドします
- 状態を変更する API には Host / Origin の検証があり、他のサイトからのクロスオリジン操作と DNS rebinding は 403 で拒否します
- 音声ファイルを外部へ送信することはありません。文字起こしは faster-whisper のローカル実行です
- UI は外部 CDN を読み込みません

## 気をつけてほしいこと

**`PODCAST_PREP_HOST` に具体的なIPアドレス（例 `192.168.1.10`）を設定すると、そのアドレスが許可され、認証のない全 API が LAN へ公開されます。** 信頼できないネットワークでは既定（localhost 専用）のまま使ってください。詳しくは README の「環境変数」を参照してください。

## 脆弱性を見つけたら

GitHub の **Issue** で報告してください。個人が趣味の範囲で運用しているツールのため、対応や返信をお約束することはできませんが、内容は必ず確認します。

深刻な問題（他者のマシンやデータに影響しうるもの）を見つけた場合は、詳細を公開する前に一度 Issue でご相談いただけると助かります。

## 既知の設計上の制約

- **認証がない** — 上記のとおり、ローカル単一ユーザーを前提とした設計です
- **書き込み先はユーザーが選ぶ** — 取込時に指定した作業フォルダに、音源・中間ファイル・書き出しが保存されます。アプリが生成したファイル以外には触れません
- **クラウド同期フォルダは作業フォルダに選べません** — 同期クライアントが書き込み途中の中間ファイルを掴むとデータが壊れるためです
